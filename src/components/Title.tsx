import React from 'react';

const Title = () => {
    return (
        <div className={'title'}>
            <h1>Gallery App</h1>
            <h2>Your pictures stored into firebase</h2>
        </div>
    );
};

export default Title;